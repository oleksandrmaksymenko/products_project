import type {NextPage} from 'next';
import PopularProduct from 'src/components/Home/PopularProduct';

const Home: NextPage = () => {
  return (
    <div>
      <PopularProduct />
    </div>
  );
};

export default Home;
